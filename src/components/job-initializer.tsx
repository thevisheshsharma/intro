"use client";

/**
 * Job initializer component that runs on the client
 * Note: The actual cron jobs run on the server side, not in the browser
 * This component is just a placeholder for future client-side job management
 */
export function JobInitializer() {
  // Jobs are initialized server-side via API routes, not client-side
  // This component can be used for client-side job status monitoring in the future
  return null
}
